<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/integration
           http://www.springframework.org/schema/integration/spring-integration-5.1.xsd">

    <!-- Gateway -->
    <int:gateway id="webServiceGateway">
        <int:method name="callWebService" request-channel="inputChannel" reply-channel="outputChannel"/>
    </int:gateway>

    <!-- Input Channel -->
    <int:channel id="inputChannel"/>

    <!-- Web Service Outbound Gateway -->
    <int-ws:outbound-gateway id="webServiceOutboundGateway"
                             request-channel="inputChannel"
                             uri="http://localhost:8080/hello"
                             marshaller="marshaller"
                             unmarshaller="marshaller"/>

    <!-- Marshaller -->
    <bean id="marshaller" class="org.springframework.oxm.jaxb.Jaxb2Marshaller">
        <property name="contextPath" value="com.example.webservice"/>
    </bean>

    <!-- Output Channel -->
    <int:channel id="outputChannel"/>

    <!-- Logger -->
    <int:service-activator input-channel="outputChannel" expression="@org.apache.commons.logging.LogFactory@getLog('WebServiceCaller').info(payload)"/>

</beans>
